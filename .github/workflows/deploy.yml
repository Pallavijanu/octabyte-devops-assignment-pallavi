name: CI/CD Pipeline

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]

jobs:

  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install dependencies
        working-directory: ./sample_app
        run: npm install

      - name: Run tests
        working-directory: ./sample_app
        run: npm test


  build:
    runs-on: ubuntu-latest
    needs: tests
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Simulate build (no DockerHub)
        run: echo "Build step skipped because no DockerHub credentials available"


  staging:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Simulate staging deploy
        run: echo "Staging deploy skipped (no AWS keys)"


  production:
    needs: staging
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Approval step placeholder
        run: echo "Manual approval simulated"

      - name: Simulate production deploy
        run: echo "Production deploy skipped (no AWS keys)"